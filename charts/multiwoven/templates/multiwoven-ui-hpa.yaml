apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "chart.fullname" . }}-ui-hpa
  namespace: {{ .Values.kubernetesNamespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "chart.fullname" . }}-ui
  minReplicas: {{ .Values.multiwovenUI.hpa.minReplicas }}
  maxReplicas: {{ .Values.multiwovenUI.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.multiwovenUI.hpa.cpuAverageUtilizationPercentageThreshold }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.multiwovenUI.hpa.memoryAverageUtilizationPercentageThreshold }}
